// src/shared/messages/events.ts

/**
 * Background -> Panel events.
 * Template currently emits none.
 * When you add real features later, define event types here and replace `never`.
 */
export type AnyEvent = never;
